name: workflow main

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  analyze-job:
    uses: ./.github/workflows/analyze.job.yaml
  test-job:
    uses: ./.github/workflows/test.job.yaml
